Flask>=3.0.0
soco>=0.30.0
yt-dlp[default]>=2024.0.0
# Philips Hue. python-mbedtls is the DTLS-PSK client for the Entertainment
# stream; its wheels stop at cp312, which is what pins this app to Python 3.12
# (Fedora 40's own version — see Containerfile). Prebuilt wheels exist for
# macosx arm64, manylinux x86_64 and musllinux; a linux/arm64 host has to build
# from sdist and needs mbedtls headers.
python-mbedtls>=2.10.1,<3
zeroconf>=0.148.0
# Beat and timbre analysis for the Hue lights. Heavy — it drags in numba,
# llvmlite and scipy for a few hundred MB — so it sits in requirements.txt
# rather than its own layer on purpose: the yt-dlp layer is *below* this one,
# and `make update-ytdlp` must not pay to reinstall LLVM to pick up an
# extractor fix.
#
# Optional at runtime. analysis.available() checks for it and, when it is
# missing, no PCM is captured and no analysis is scheduled — the app plays
# audio exactly as before and only the lights go dark. That is what makes it
# safe to skip on a platform where numba has no wheel.
librosa>=1.0.0
